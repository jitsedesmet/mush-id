name: Build-and-deploy


concurrency:
  group: 'pages'
  cancel-in-progress: true

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    uses: ./.github/workflows/build-might-deploy.yml
    with:
      deploy: true
    permissions:
      contents: read
      packages: write
